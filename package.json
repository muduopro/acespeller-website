{
  "name": "acespeller-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync:legal": "mkdir -p public && cp ../dictation-helper/privacy_policy.md public/ 2>/dev/null || true && cp ../dictation-helper/terms_of_service.md public/ 2>/dev/null || true",
    "predev": "npm run sync:legal",
    "prebuild": "npm run sync:legal",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "dependencies": {
    "firebase": "^12.11.0"
  }
}
